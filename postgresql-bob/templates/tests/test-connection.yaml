apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "postgresql-bob.fullname" . }}-test-connection"
  labels:
    {{- include "postgresql-bob.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "postgresql-bob.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
